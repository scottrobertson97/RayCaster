{
  "name": "raycaster",
  "version": "1.0.0",
  "description": "",
  "main": "bullet.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --format=esm --sourcemap",
    "watch": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --format=esm --sourcemap --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
